node_modules
dist
dist-electron
release
build/*.ico
*.log
.DS_Store
.env
.env.local

# build / tooling artifacts
tsconfig.tsbuildinfo
*.tsbuildinfo

# generated transpile output of vite.config.ts (source of truth is the .ts)
vite.config.js
vite.config.d.ts

# local SQLite database (created at runtime in userData; never commit data)
*.db
*.db-wal
*.db-shm
*.sqlite

# generated by `npm run i18n:extract` (input for expanding src/lib/bn/dict.ts)
scripts/strings.txt

# scraped reference markup from another vendor's POS, kept locally for comparison
things.html
